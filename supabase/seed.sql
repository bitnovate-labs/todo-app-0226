-- Optional seed data. Run after schema.sql if you need test data.
-- Example: insert a test profile (replace UUID with a real auth.users id after sign-up).

-- insert into public.profiles (id, email, full_name)
-- values (
--   '00000000-0000-0000-0000-000000000000'::uuid,
--   'test@example.com',
--   'Test User'
-- )
-- on conflict (id) do nothing;
